import { easyBoardState } from '../globals'
import Board from './Board'

describe('Constructor', ()=> {
  it('should load', () =>{
    // const board = new Board(easyBoardState)
    
    // expect(board.numberOfMines).toEqual(3)
  })
})
